---
name: klao
version: "1.1.0"
license: MIT
description: เกลาข้อความให้อ่านเหมือนคนเขียน — ตัด "สำนวน AI" (em-dash, ไม่ใช่ X แต่ Y, closer สำเร็จรูป, adverb ฟุ้ง, tricolon) และตัดคำที่อธิบายซ้ำกับสิ่งที่จอ/บริบทบอกอยู่แล้ว โดยไม่เปลี่ยนความหมายและไม่แตะ logic. Use when the user says เกลาคำ / เกลาสำนวน / ตัดสำนวน AI / ปรับข้อความ / "อ่านแล้วเหมือน AI เขียน" / ยาวไป / เยิ่นเย้อ / copy edit / de-AI, or asks to shorten UI strings, docs, or prose without changing behaviour. Not for: แปลภาษา, เขียนเนื้อหาใหม่, หลบ AI detector. กฎเฉพาะราย repo (ถ้ามี) อยู่ใน references/repos.local.md.
---

# /klao — เกลาคำ

> `klao` = "เกลา" คำที่ทีมใช้เรียกงานนี้อยู่แล้ว

งานนี้คือ **prose อย่างเดียว** ไม่ใช่ refactor. เป้าคือข้อความเดิมความหมายเดิม แต่สั้นลงและ
อ่านแล้วเป็นคนพูด ไม่ใช่เครื่องเขียน.

หลักที่ทุกกฎข้างล่างมาจากอันเดียวกัน: **ตัดสิ่งที่ผู้อ่านรู้อยู่แล้วจากที่อื่น** สีของเซลล์ ปุ่มที่อยู่ตรงนั้น
ชื่อหน้า ตัวเลขข้าง ๆ หรือประโยคก่อนหน้า ล้วนพูดแทนได้ — คำที่พูดซ้ำสิ่งเหล่านั้นคือคำที่ตัดได้.

## 1. ขอบเขต — ถามหรือสรุปให้ชัดก่อนแก้

ถ้า user ไม่ได้ระบุ ให้สรุปขอบเขตออกมาเป็นบรรทัดเดียวแล้วลงมือเลย ไม่ต้องรอตอบ:

- **แก้ไฟล์ไหน** — ทั้ง repo, หน้าเดียว, ไฟล์เดียว, หรือแค่ diff ที่ค้างอยู่ (`git diff`)
- **ภาษาไหน** — ไทย user-visible / อังกฤษ prose (docs, commit, PR) กฎคนละชุด ดู §3–4
- **user-visible เท่านั้น** เป็น default เสมอ

### แก้เองตรงไหน เสนอตรงไหน

> ข้อสังเกตจากเซสชันเดียว (2026-07-30) ยังไม่มีตัวเลขรองรับ — รอรอบหน้ามายืนยันหรือหักล้าง

ในรอบที่เจ้าของกับ AI เกลาเอกสารเดียวกันคนละรอบ ผลแยกเป็นสองกองชัดเจน และเส้นแบ่ง**ไม่ใช่**
ใหญ่กับเล็ก (`เสื้อคลุม` เป็นคำเดียวแต่ AI ตัดสินผิด):

| คลาส | ตัวอย่างจริง | ใครถูก | ท่าที่ควรใช้ |
|---|---|---|---|
| **ตรวจได้กับกฎภาษา หรือกับอีกส่วนของเอกสารเดียวกัน** | `ที่มีจำกัด` ไม่รับ `ที่` · `ที่เหมือนกัน` เกาะคำนามผิดตัว · โซ่คำ `ขอบ` ขาด · ศัพท์เสียนิยาม · เลขลอย · `กฏ` · ทับศัพท์สองรูป · `ๆ` ไม่เว้นวรรค | AI | **แก้เลย แล้วรายงานเป็นตาราง** |
| **ตัดสินแทนคนอ่าน** | อุปมาที่ต้องถอดรหัส · หัวข้อกำกวม (`ดูตัว` = ดูว่าที่คู่ครองด้วย) · ท่าสั่งสอน · โครงลิสต์ที่คนนับตามไม่ทัน · จะตัดเนื้อหาไหน | เจ้าของ | **เสนอพร้อมเหตุผล ไม่แก้เอง** |

คำถามที่ใช้แยก: **ตัดสินอันนี้ได้ด้วยการเปิดดูของที่มีอยู่ หรือต้องเดาว่าคนอ่านจะรู้สึกอย่างไร**
ถ้าต้องเดา ให้เสนอ — เจ้าของรู้ว่าใครจะอ่าน AI ไม่รู้

⚠️ **กองซ้ายไม่ใช่ใบอนุญาตให้แก้เงียบ ๆ** §7 ยังบังคับให้ทุกแถวมีตาราง before → after ที่โต้แย้งได้
ต่างกันแค่ กองซ้ายคือ "แก้แล้วรายงาน" กองขวาคือ "ยังไม่แก้ รอเคาะ"

## 2. สแกนก่อนแก้

**สตริง UI** — ด่านแรกคือ em-dash:
```sh
# em-dash / en-dash — ตัดบรรทัด comment ทิ้ง แล้วเอาเฉพาะบรรทัดที่มีอักษรไทย
grep -rn "—\|–" <path> | grep -vE ':[0-9]+: *(//|\*|/\*)' | grep '[ก-๙]'
# ขีดเปล่าแทนค่าว่าง + select placeholder
grep -rn "'—'\|\"—\"\|— .* —" <path>
```

**เนื้อหาไทยยาว** — ด่านแรกคืออักขรวิธี (หมวด N): ในเนื้อหาไทยที่ AI ร่าง 828k อักขระ
เจอ em-dash 2 ครั้ง แต่มหัพภาคท้ายประโยค 1,134 ครั้ง
```sh
grep -rnE '[ก-๙]\.' <path>                 # มหัพภาคท้ายประโยคไทย
grep -rnE '[ก-๙]ๆ' <path>                  # ๆ ไม่มีเว้นวรรคหน้า
grep -rnE '[ก-๙] ?[0-9]+ ?[ก-๙]' <path>    # เลขอารบิกกลาง prose (ใน label ไม่นับ)
# hard — เจอแล้วแก้ (TH-PR-05 ตัวเร่ง · TH-PR-03 closer · TH-PR-11 dummy subject)
grep -rnE "อย่างยิ่ง|อย่างแน่นอน|นั่นคือ|มันคือ|ไม่ใช่แค่" <path>
# soft (I–M) — เป็น tell เมื่อซ้ำเท่านั้น · คำที่ FP คุ้มครองไว้แล้วไม่อยู่ในบรรทัดนี้
grep -rnE "อีกทั้ง|ยกระดับ|ขับเคลื่อน|ทุกคน|ไม่มีใคร|อย่างลงตัว|ในยุคที่" <path>
# hard — สะกดผิด (TH-OR-05) ไม่มี register ที่ถูก เจอแล้วแก้
# `กฏ` ต้องมี lookbehind ไม่งั้นติด `ปรากฏ` ทุกครั้ง — BSD grep -E ทำไม่ได้ ใช้ rg -P
rg -nP "(?<!ปรา)กฏ|อนุญาติ|ลายเซ็นต์|ผลลัพท์|สังเกตุ|โน๊ต|เว็ปไซต์|อาทิตย์ที่แล้ว์|ปรากฏการ์ณ" <path>
# hard — ท่าบอกจุดยืน (TH-PR-16) · ตัดวลีแล้วประโยคที่เหลือต้องยืนได้เอง
grep -rnE "คนมักเข้าใจผิด|ข้อนี้สำคัญ|จุดสำคัญคือ|ที่น่าสนใจคือ|ตรงนี้คือหัวใจ" <path>
```

⚠️ **grep หมวด L ได้ 0 ไม่ใช่การผ่านด่าน** (postmortem 2026-07-29 พลาดสองรอบติด) L เรียงเป็น
รายการคำ แต่เกณฑ์จริงคือ **ผู้อ่านต้องแปลอีกชั้นไหม** ซึ่ง grep ไม่รู้จัก — หลังรัน grep ต้อง**ไล่
อุปมารูปธรรมทุกตัวในงานด้วยตา** แล้วแยกสามกอง: ผ่านเพราะไทยมีสำนวนนั้นเอง (`รากเหง้า` ·
`ต้นทาง`/`ปลายทาง`) · ก้ำกึ่งเสนอให้เจ้าของเคาะ · ยืมมาหรือประกอบขึ้นเองแก้เลย
**อุปมาที่ผู้เขียนคิดสด ๆ ก็ต้องแก้** ไม่ได้รอดเพราะไม่ใช่คำแปล (`tells-th.md` หมวด L2)

⚠️ **ผลจาก grep เป็นรายชื่อผู้ต้องสงสัย ไม่ใช่รายการงาน** ของจริงมักเหลือหลักหน่วย
(ตัวเลขจริงใน `references/tells-th.md` หมวด A และ N) **ต้องเปิดดูทีละบรรทัดว่าอยู่ใน string
ที่ render จริงไหม** แล้วแจ้งจำนวนที่เหลือหลังกรอง · **ห้าม sed ทั้งไฟล์** แต่ละจุดบริบทต่างกัน

## 3. ไทย user-visible — กฎ

รายละเอียด + ตาราง before/after ของจริง: `references/tells-th.md` (อ่านเมื่อเจองานเนื้อหายาว
หรือไม่แน่ใจว่าจะตัดท่าไหน)

หัวใจ 6 ข้อ:

1. **em-dash ในสตริงที่ผู้ใช้เห็น = 0** ไทยไม่ใช้ em-dash. ท่าที่ AI ชอบคือ "เกริ่น — ขยายความ"
   ตัดโดย: เว้นวรรคเฉย ๆ · ใช้คำเชื่อมไทย (คือ/ที่/จึง/แล้ว/เพราะ) · แยกประโยค · หรือ**ตัดหางทิ้ง**
   ถ้าหางแค่พูดซ้ำสิ่งที่จอบอกอยู่แล้ว
2. **ขีดเปล่า `—` ที่ใช้แทนค่าว่าง ห้ามใช้** ใส่คำจริง (`ไม่ระบุ` / `ไม่พบสมาชิก` / `ไม่มีข้อมูล`)
   หรือถ้าที่ถูกคือไม่ต้องแสดงบรรทัดนั้น ให้ conditional render ไปเลย
3. **`— ทีม —` แบบ select placeholder → คำสั่ง** (`เลือกทีม`, `เลือกตำแหน่ง`)
4. **ตัดคำทักทาย/หยอด** ("ลองคุยกันดู", "พักผ่อนบ้างนะ", "ได้เลย!", "ไม่งั้นตั้งไว้ก็ไม่มีอะไรมา")
5. **เอาข้อสรุปขึ้นก่อน** อย่าเปิดด้วยเงื่อนไขแล้วค่อยเฉลย
   `บันทึกไม่ได้ — X กำลังแก้ไข` → `X กำลังแก้ไขอยู่ บันทึกไม่ได้`
6. **ตัดคำที่บริบทถืออยู่แล้ว** `ของคุณ` `ของวันนี้` `เซิร์ฟเวอร์` `ระบบ` `รอบแรก`
   `อีเมลของคุณเปลี่ยนแล้ว` → `อีเมลเปลี่ยนแล้ว`

## 4. อังกฤษ (docs / commit body / PR / README) — กฎคนละชุด

⚠️ **อย่าเอากฎ em-dash ไปใช้กับ prose อังกฤษ** — em-dash ในภาษาอังกฤษถูกต้อง
และอาจเป็นเสียงเจ้าของอยู่แล้ว (ดูรายการ "ไม่ใช่ tell" ท้าย `references/tells-en.md`)
ที่ต้องตัดคือของอื่นในไฟล์เดียวกัน

## 5. ห้ามแตะ

รายการเต็ม 28 ข้อพร้อม keep rate: `references/false-positives.md` — **ทุกข้อมีบรรทัด
"เป็น tell จริงเมื่อ" ที่บอกว่าข้อยกเว้นสิ้นสุดตรงไหน อ่านบรรทัดนั้นก่อนตัดสิน** ถ้าเข้าข้อยกเว้น
แล้วยังอยากแก้ (เช่น keep rate สูงแต่ในไฟล์นี้อ่านแล้วสะดุดจริง) ให้เสนอพร้อมเหตุผล อย่าแก้เงียบ ๆ

- **code comment** — เกลาเฉพาะสิ่งที่ผู้ใช้เห็น
- **en-dash ในช่วงตัวเลข/เวลา/วัน** `08:00–20:00`, `จ–ศ`, ช่วงวันที่ของงวด — อันนี้ถูกแล้ว
  (แค่ช่วงเท่านั้น: `จังหวะการรุก–รับ` เป็นคู่ความคิด ไม่ใช่ช่วง ตัวนั้นเขียนเป็น `และ`)
- **`·` (middle dot)** เป็นตัวคั่น label ไม่ใช่สำนวน เก็บไว้
- **ศัพท์เฉพาะ/ชื่อฟีเจอร์/ชื่อปุ่ม** ที่อ้างถึงของจริงในจอ อย่าเปลี่ยนคำจนไม่ตรงกับปุ่ม
- **ศัพท์ของศาสตร์ที่ตัวคำเป็นอุปมาของภาษาต้นทางเอง** `กิ่ง` ใน 12 กิ่ง มาจาก 支 ของ `天干地支`
  ห้ามเปลี่ยนคำ — แต่ถ้าใช้แล้วไม่นิยาม หรือชนกับอุปมาของผู้เขียนเอง ให้แก้ที่โครง (FP ข้อ 28)
- **ความหมาย** ถ้าไม่แน่ใจว่าประโยคหมายถึงอะไร (โดยเฉพาะเนื้อหาทำนาย/กฎธุรกิจ) **ทำเครื่องหมายไว้
  ถามเจ้าของ อย่าเดา**
- **logic** ยกเว้นการซ่อนของที่ไม่ควรแสดง: conditional render ระดับบรรทัด (TH-UI-03)
  หรือระดับ section เมื่อทั้งก้อนมีแต่ empty state ของฟีเจอร์รอง (TH-UI-09) — ถ้าเป็น
  onboarding CTA ที่ product ตั้งใจดัน **ถามก่อน**

## 6. ตรวจก่อนจบ

กฎ + gate เฉพาะราย repo: อ่าน `references/repos.local.md` **ถ้ามี** (ไฟล์ส่วนตัว gitignored —
เขียนเองตามโครงใน `references/repos.md`) ถ้าไม่มีก็ใช้กฎกลางในไฟล์นี้

อย่างน้อยเสมอ:

```sh
grep -rn "—\|–" <path> | grep -vE ':[0-9]+: *(//|\*|/\*)' | grep '[ก-๙]'   # เหลือเฉพาะที่ตั้งใจ
grep -rn "'—'\|\"—\"" <path>               # ขีดเปล่าแทนค่าว่าง — **ตัวกรองไทยข้างบนมองไม่เห็น**
grep -rnE '[ก-๙]\.|[ก-๙]ๆ' <path>          # อักขรวิธี (หมวด N) — หักอักษรย่อ (ชม. พ.ศ.) ออกก่อน
# TH-OR-05 สะกดผิด — ต้องเหลือ 0 · lookbehind กัน `ปรากฏ` ซึ่งสะกดถูก (ก + ฏ พอดี)
rg -nP "(?<!ปรา)กฏ|อนุญาติ|ลายเซ็นต์|ผลลัพท์|สังเกตุ|โน๊ต|เว็ปไซต์|อาทิตย์ที่แล้ว์|ปรากฏการ์ณ" <path>
# แล้วรัน typecheck / lint / test ของ repo นั้น
```

### ด่านข้ามเอกสาร — บังคับทุกรอบของงานเนื้อหายาว ไม่ใช่แค่ตอนตัดเนื้อหา

ทุกด่านข้างบนตรวจ**ทีละบรรทัด** คือ grep เจอแล้วเปิดดูบรรทัดนั้น ไม่มีด่านไหนถือเอกสารทั้งฉบับ
ไว้พร้อมกัน — ของจริงพลาดคลาสนี้ **3 ครั้งในเซสชันเดียว** และมีครั้งเดียวที่เกิดจากการตัดเนื้อหา
อีกสองครั้งเกิดจากการแก้คำเฉย ๆ:

1. **ศัพท์เฉพาะยังมีที่นิยามอยู่ไหม และนิยามมาก่อนใช้ครั้งแรกหรือเปล่า**
   `ก้านกิ่ง` เสียนิยามเพราะย่อหน้าที่อธิบายมันถูกตัดตอนย่อบทความ ทั้งที่ตัวคำยังอยู่สองจุด
   → ท่าที่ทนการตัดที่สุดคือยัดนิยามลงในวงเล็บที่ตัวรายการ เพราะวงเล็บไปไหนก็ไปกับคำ
2. **คำบอกจำนวนยังนับตรงกับลิสต์ที่เหลือไหม**
   `และ 3 สายเป็นการนับแบบหยาบ` กลายเป็นเลขลอยหลังเปลี่ยนโครงลิสต์เป็น "หนึ่งบวกสอง" (TH-PR-15)
   → `grep -rnoE "(สอง|สาม|สี่|ห้า|หก)(สาย|อย่าง|ข้อ|คู่|แบบ|ตระกูล)" <path>` แล้วทำซ้ำกับเลขอารบิก
3. **ศัพท์ที่ย่อหน้าสรุปประกาศไว้ ยังใช้คำเดิมในหัวข้อและในเนื้อไหม** (TH-PR-17 · S2)
   หัวข้อเลื่อนไปใช้คำพ้องความ ห่างจากสรุป 38 บรรทัด ผู้อ่านต้องจับคู่เอง
   → ดึงศัพท์หลักจากย่อหน้าสรุปมาเป็นลิสต์ แล้ว grep ทีละตัวว่าโผล่กี่รูป

ทั้งสามข้อ grep คัดได้แค่ผู้ต้องสงสัย **ตัวตัดสินคือเปิดย่อหน้าสรุปกับหัวข้อขึ้นมาอ่านเทียบกันเอง**

⚠️ บรรทัดที่สองไม่ใช่ของแถม **`grep '[ก-๙]'` ทำให้ด่านนี้มองไม่เห็น TH-UI-03 แม้แต่จุดเดียว**
เพราะ `roleLabel ?? '—'` ไม่มีอักษรไทยบรรทัดเดียวกัน (วัดจริง: ขีดเปล่า 2 จุดรอดรอบกวาด
ที่ฆ่า em-dash 18 ตัว) · `<path>` ต้องตัด `.claude/` `node_modules/` ไม่งั้น worktree
ในตัว repo ให้ผลคูณสาม

**แทนคำเป็นชุดแล้วต้องอ่านผลลัพธ์กลับทีละจุด** มันพังกลางคำได้ — ของจริงจาก corpus:
`ท่าน`→`คุณ` ทำให้ `เท่านั้น` กลายเป็น `เคุณั้น` แล้ว ship ไปแล้ว

ถ้าไฟล์เนื้อหามี test gate อยู่แล้ว ต้องเขียว. ถ้าเป็นเนื้อหาชุดใหม่ที่ AI ร่าง
**เสนอเพิ่ม gate test** ให้ล็อกไว้ไม่ให้กลับมา: ห้าม em-dash สำหรับสตริง UI · อักขรวิธีหมวด N
สำหรับเนื้อหาไทยยาว ซึ่งเป็นด่านที่ของจริงอยู่ (ตัวอย่างใน `references/repos.md`)

## 7. รายงานกลับ

ตอบเป็นภาษาไทย และให้ **ตาราง before → after ทุกบรรทัดที่แก้** เจ้าของต้องอ่านแล้ว veto ทีละอันได้
อย่าสรุปว่า "เกลาแล้ว 40 จุด" เฉย ๆ.

**ทุกแถวต้องอ้าง rule ID** (`ไฟล์:บรรทัด | ก่อน | หลัง | rule`) แถวที่อ้าง ID ไม่ได้ = ยังไม่มี
กฎรองรับ ให้ถามก่อนแก้ · ID อยู่ใต้หัวข้อแต่ละหมวดใน `references/tells-*.md` พร้อมระดับ
(`hard` เจอแล้วแก้ · `soft` เป็น tell เมื่อซ้ำ) และที่มา: `TH-UI` สตริงในจอ · `TH-PR` prose ไทย ·
`TH-OR` อักขรวิธี · `EN` อังกฤษ

พร้อมบอกด้วยว่า **อะไรที่จงใจไม่แตะ** และเพราะอะไร (ย่อหน้า "Untouched on purpose:" —
เช่น "code comments และ en-dash ในช่วงเวลา อันนั้นถูกแล้ว")

## 8. Commit

- แก้คำล้วน → `copy(scope): …` (เช่น `copy(settings): หัวข้อสั้นลง คำสั่งขึ้นก่อนเงื่อนไข`)
- ไปพร้อมงาน UI อื่น → `fix(ui): …`
- subject เป็นประโยคที่บอกว่าตอนนี้ของมันทำอะไร ไม่ใช่ชื่องาน
- body เป็น prose บอกเหตุผล + ยกตัวอย่าง before/after ที่เด่นที่สุดหนึ่งอัน + ย่อหน้า "จงใจไม่แตะ"
